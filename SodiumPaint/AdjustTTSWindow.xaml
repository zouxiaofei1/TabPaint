<Window x:Class="SodiumPaint.AdjustTTSWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="调整色温/色调/饱和度" Height="220" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- 色温 -->
		<StackPanel Grid.Row="0" Orientation="Vertical">
			<TextBlock Text="色温 (Temperature)" />
			<Slider x:Name="TemperatureSlider"
					Minimum="-100" Maximum="100" Value="0"
					TickFrequency="10" IsSnapToTickEnabled="True"
					ValueChanged="Slider_ValueChanged"/>
		</StackPanel>

		<!-- 色调 -->
		<StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,0">
			<TextBlock Text="色调 (Tint)" />
			<Slider x:Name="TintSlider"
					Minimum="-100" Maximum="100" Value="0"
					TickFrequency="10" IsSnapToTickEnabled="True"
					ValueChanged="Slider_ValueChanged"/>
		</StackPanel>

		<!-- 饱和度 -->
		<StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10,0,0">
			<TextBlock Text="饱和度 (Saturation)" />
			<Slider x:Name="SaturationSlider"
					Minimum="-100" Maximum="100" Value="0"
					TickFrequency="10" IsSnapToTickEnabled="True"
					ValueChanged="Slider_ValueChanged"/>
		</StackPanel>

		<!-- 按钮 -->
		<StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="确定" Width="80" IsDefault="True" Click="Ok_Click" Margin="0,0,10,0"/>
			<Button Content="取消" Width="80" IsCancel="True" Click="Cancel_Click"/>
		</StackPanel>
	</Grid>

</Window>
