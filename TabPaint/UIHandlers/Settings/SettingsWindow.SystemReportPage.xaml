<UserControl x:Class="TabPaint.Pages.SystemReportPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="500"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/SubWindowStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ReportTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                                  Focusable="false"
                                                  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                                  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                        <ScrollViewer.Resources>
                                            <Style TargetType="ScrollBar" BasedOn="{StaticResource FluentScrollBar}"/>
                                        </ScrollViewer.Resources>
                                        <ScrollViewer.Template>
                                            <ControlTemplate TargetType="ScrollViewer">
                                                <Grid Background="{TemplateBinding Background}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                                            Grid.Column="0" Grid.Row="0"
                                                                            Margin="{TemplateBinding Padding}"
                                                                            Content="{TemplateBinding Content}"
                                                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                            CanContentScroll="{TemplateBinding CanContentScroll}"/>
                                                    <ScrollBar x:Name="PART_VerticalScrollBar"
                                                               Grid.Column="1" Grid.Row="0"
                                                               Orientation="Vertical"
                                                               ViewportSize="{TemplateBinding ViewportHeight}"
                                                               Maximum="{TemplateBinding ScrollableHeight}"
                                                               Minimum="0"
                                                               Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                               Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                                                    <ScrollBar x:Name="PART_HorizontalScrollBar"
                                                               Grid.Column="0" Grid.Row="1"
                                                               Orientation="Horizontal"
                                                               ViewportSize="{TemplateBinding ViewportWidth}"
                                                               Maximum="{TemplateBinding ScrollableWidth}"
                                                               Minimum="0"
                                                               Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                               Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                                                    <Border Grid.Column="1" Grid.Row="1" 
                                                            Background="{TemplateBinding Background}"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </ScrollViewer.Template>
                                    </ScrollViewer>

                                    <Border x:Name="FocusBorder"
                                            BorderBrush="{DynamicResource SystemAccentBrush}"
                                            BorderThickness="0,0,0,2"
                                            CornerRadius="4"
                                            Visibility="Collapsed"
                                            VerticalAlignment="Bottom"
                                            Height="2"/>
                                </Grid>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource ControlMouseOverBackgroundBrush}"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BorderFocusBrush}"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="true">
                                    <Setter TargetName="FocusBorder" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource SystemAccentBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource ControlDisabledBackgroundBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid ContextMenuOpening="Grid_ContextMenuOpening" Background="Transparent">
        <Grid.ContextMenu>
            <ContextMenu x:Name="PageContextMenu" Style="{StaticResource Win11ContextMenuStyle}">
                <MenuItem x:Name="MenuPin" Click="MenuPin_Click" Style="{StaticResource Win11MenuItemStyle}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path x:Name="PinIcon" Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12M8.8,14L10,12.8V4H14V12.8L15.2,14H8.8Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ContentPresenter}}" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform"/>
                            <TextBlock x:Name="TxtPin" Text="{DynamicResource L_Settings_PinPage}"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>

        <!-- Pin Button -->
        <ToggleButton x:Name="BtnPinTop" 
                      Style="{StaticResource FluentToggleButtonStyle}"
                      HorizontalAlignment="Right" VerticalAlignment="Top" 
                      Margin="0,20,20,0"
                      Width="32" Height="32"
                      Panel.ZIndex="10"
                      Click="MenuPin_Click"
                      ToolTip="{DynamicResource L_Settings_PinPage}">
            <Path x:Name="PinIconTop" 
                  Data="M16,12V4H17V2H7V4H8V12L6,14V16H11.2V22H12.8V16H18V14L16,12M8.8,14L10,12.8V4H14V12.8L15.2,14H8.8Z" 
                  Fill="{DynamicResource IconFillBrush}" 
                  Width="14" Height="14" Stretch="Uniform"/>
        </ToggleButton>
        <StackPanel Margin="24,24,44,24">
            <TextBlock Text="{DynamicResource L_Settings_Advanced_CollectReport_Title}"
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8"
                   Foreground="{DynamicResource TextPrimaryBrush}"/>

        <TextBlock Text="{DynamicResource L_Settings_Advanced_CollectReport_Desc}"
                   Margin="0,0,0,12"
                   Foreground="{DynamicResource TextTertiaryBrush}"
                   TextWrapping="Wrap"/>

        <Grid Margin="0,0,0,10">
            <Border Background="{DynamicResource ControlBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                <TextBox x:Name="ReportTextBox"
                         Style="{StaticResource ReportTextBoxStyle}"
                         MinHeight="280"
                         BorderThickness="0"
                         Background="Transparent"
                         FontFamily="Consolas"
                         FontSize="12"
                         Padding="12"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         IsReadOnly="True"/>
            </Border>

            <Border x:Name="LoadingOverlay"
                    Background="#77000000"
                    CornerRadius="6"
                    Visibility="Collapsed">
                <StackPanel Width="240"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Height="4"/>
                    <TextBlock Text="Generating system report..."
                               Margin="0,10,0,0"
                               HorizontalAlignment="Center"
                               Foreground="White"
                               FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>

        <DockPanel LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center"
                       Foreground="{DynamicResource TextTertiaryBrush}"
                       Text="{DynamicResource L_Settings_Advanced_CollectReport_Tip}"/>


            <Button x:Name="CopyButton" DockPanel.Dock="Right" Width="120" Height="32"
                    Content="{DynamicResource L_Settings_Advanced_CollectReport_Copy}"
                    Click="Copy_Click"
                    Style="{StaticResource Win11SecondaryButtonStyle}"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
