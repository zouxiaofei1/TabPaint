<UserControl x:Class="TabPaint.Pages.AboutPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TabPaint"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500"
             Background="Transparent">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/SubWindowStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Margin="24,24,44,24">
        <Border Background="Transparent" 
                HorizontalAlignment="Center"
                MaxWidth="400">

            <StackPanel HorizontalAlignment="Center">
                <Image x:Name="AppIcon" 
                       Source="pack://application:,,,/Resources/TabPaint.ico" 
                       Width="84" Height="84" 
                       HorizontalAlignment="Center" 
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Margin="0,0,0,16"/>
                <TextBlock Text="TabPaint" 
                           HorizontalAlignment="Center"
                           FontSize="22" 
                           FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="{DynamicResource L_Settings_About_Subtitle}" 
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}" 
                           FontSize="12" 
                           Margin="0,8,0,16"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Button Cursor="Hand"
                            Margin="0,0,8,0"
                            Padding="8,4"
                            Height="Auto"
                            MinHeight="0"
                            Background="{DynamicResource GlassBackgroundMediumBrush}"
                            BorderBrush="{DynamicResource BorderLightBrush}"
                            BorderThickness="1"
                            Tag="https://github.com/zouxiaofei1"
                            Click="OnOpenUrlClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource L_Settings_About_Developer}" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,4,0" FontSize="11"/>
                            <TextBlock Text="zouxiaofei1" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <Button Cursor="Hand"
                            Padding="8,4"
                            Height="Auto"
                            MinHeight="0"
                            Background="{DynamicResource GlassBackgroundMediumBrush}"
                            BorderBrush="{DynamicResource BorderLightBrush}"
                            BorderThickness="1"
                            Click="OnCheckUpdateClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Ver." Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,4,0" FontSize="11"/>
                            <TextBlock Text="{Binding ProgramVersion, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                       Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                    <Button Cursor="Hand"
                            Margin="8,0,0,0"
                            Padding="8,4"
                            Height="Auto"
                            MinHeight="0"
                            Background="{DynamicResource GlassBackgroundMediumBrush}"
                            BorderBrush="{DynamicResource BorderLightBrush}"
                            BorderThickness="1"
                            Click="OnOpenAgreementClick">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{DynamicResource Agreement_Image}"  Width="13"  Height="13" Stretch="Uniform"  Fill="{DynamicResource IconFillBrush}"
                                  Margin="8,0,4,0"/>
                            <TextBlock Text="MIT"
                                       FontSize="11" Margin="0,0,8,0"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Button>

                </StackPanel>

            </StackPanel>
        </Border>
        <Separator Margin="0,15" Background="{DynamicResource BorderLightBrush}" Width="400" HorizontalAlignment="Center"/>
        <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Width="300">
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                    <Setter Property="Height" Value="36"/>
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" 
                                        Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                        CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource ChromeMediumBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </StackPanel.Resources>
            <Button Cursor="Hand" 
                    Background="{DynamicResource GlassBackgroundMediumBrush}"
                    Tag="https://github.com/zouxiaofei1/TabPaint" 
                    Click="OnOpenUrlClick">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{DynamicResource GitHub_Image}"  Width="13"  Height="13" Stretch="Uniform"  Fill="{DynamicResource IconFillBrush}"
Margin="0,0,8,0"/>
                    <TextBlock Text="{DynamicResource L_Settings_About_Github}"/>
                </StackPanel>
            </Button>
            <Button Cursor="Hand" 
                    Background="{DynamicResource GlassBackgroundMediumBrush}"
                    Tag="https://github.com/zouxiaofei1/TabPaint/releases" 
                    Click="OnOpenUrlClick">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets" Margin="0,1,8,0"/>
                    <TextBlock Text="{DynamicResource L_Settings_About_CheckUpdate}"/>
                </StackPanel>
            </Button>
            <Button Cursor="Hand" 
                    Background="{DynamicResource GlassBackgroundMediumBrush}"
                    Tag="https://zouxiaofei1.github.io/tabpaint-site/guide/start/installation.html" 
                    Click="OnOpenUrlClick">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE897;" FontFamily="Segoe MDL2 Assets" Margin="0,1,8,0"/>
                    <TextBlock Text="{DynamicResource L_Settings_About_Help}"/>
                </StackPanel>
            </Button>
            <Button Cursor="Hand" 
                    Background="{DynamicResource GlassBackgroundMediumBrush}"
                    Tag="https://tabpaint.cc/" 
                    Click="OnOpenUrlClick">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{DynamicResource HomePage_Image}"  Width="13"  Height="13" Stretch="Uniform"  Fill="{DynamicResource IconFillBrush}"
Margin="0,0,8,0"/>
                    <TextBlock Text="{DynamicResource L_Settings_About_HomePage}"/>
                </StackPanel>
            </Button>

            <Button Cursor="Hand" 
                    Background="{DynamicResource GlassBackgroundMediumBrush}"
                    Tag="https://tabpaint.cc/guide/version_history.html" 
                    Click="OnOpenUrlClick">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{DynamicResource Docs_Image}"  Width="13"  Height="13" Stretch="Uniform"  Fill="{DynamicResource IconFillBrush}"
Margin="0,0,8,0"/>
                    <TextBlock Text="{DynamicResource L_Settings_About_Changelog}"/>
                </StackPanel>
            </Button>

            <Button Cursor="Hand" 
                    Background="{DynamicResource GlassBackgroundMediumBrush}"
                    Tag="https://github.com/zouxiaofei1/TabPaint/issues" 
                    Click="OnOpenUrlClick">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{DynamicResource Report_Image}"  Width="13"  Height="13" Stretch="Uniform"  Fill="{DynamicResource IconFillBrush}"
             Margin="0,0,8,0"/>
                    <TextBlock Text="{DynamicResource L_Settings_About_Issue}"/>
                </StackPanel>
            </Button>
        </StackPanel>

    </StackPanel>
</UserControl>
