<Window x:Class="TabPaint.Windows.StickyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StickyNote" Height="300" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        AllowDrop="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseEnter="Window_MouseEnter"
        MouseLeave="Window_MouseLeave"
        MouseDoubleClick="Window_MouseDoubleClick"
        MouseWheel="Window_MouseWheel"
        PreviewKeyDown="Window_PreviewKeyDown"
        DragOver="Window_DragOver"
        Drop="Window_Drop">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/SubWindowStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="StickyCloseButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="#E6FFFFFF"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground" Value="White"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <!-- 将 ContextMenu 加在这个 Border 上 -->
        <Border x:Name="MainBorder" 
                BorderBrush="#40808080" BorderThickness="1" Background="#01FFFFFF">

            <Border.ContextMenu>
                <!-- 应用 Win11 样式 -->
                <ContextMenu Style="{DynamicResource Win11ContextMenuStyle}">

                    <!-- 置顶选项 (默认为选中，因为窗口初始化是 TopMost=True) -->
                    <MenuItem Header="{DynamicResource L_Stick_TopMost}" 
                              IsCheckable="True" 
                              IsChecked="True"
                              Click="OnTopMostClick"
                              Style="{DynamicResource Win11MenuItemStyle}"/>

                    <!-- 透明度子菜单 -->
                    <MenuItem Header="{DynamicResource L_Stick_Opacity}" 
                              Style="{DynamicResource Win11MenuItemStyle}">
                        <MenuItem.Icon>
                            <TextBlock Text="&#xE793;" FontFamily="Segoe MDL2 Assets" FontSize="12"/>
                        </MenuItem.Icon>
                        <MenuItem Header="100%" Tag="1.0" Click="OnOpacityChangeClick" Style="{DynamicResource Win11MenuItemStyle}"/>
                        <MenuItem Header="80%"  Tag="0.8" Click="OnOpacityChangeClick" Style="{DynamicResource Win11MenuItemStyle}"/>
                        <MenuItem Header="60%"  Tag="0.6" Click="OnOpacityChangeClick" Style="{DynamicResource Win11MenuItemStyle}"/>
                        <MenuItem Header="40%"  Tag="0.4" Click="OnOpacityChangeClick" Style="{DynamicResource Win11MenuItemStyle}"/>
                        <MenuItem Header="20%"  Tag="0.2" Click="OnOpacityChangeClick" Style="{DynamicResource Win11MenuItemStyle}"/>
                    </MenuItem>

                    <!-- 重置缩放 (实用小功能) -->
                    <MenuItem Header="{DynamicResource L_Stick_ResetScale}" 
                              Click="OnResetScaleClick"
                              Style="{DynamicResource Win11MenuItemStyle}">
                    <MenuItem.Icon>
                            <Path Data="{DynamicResource Undo_Image}" Stretch="Uniform" Fill="{DynamicResource IconFillBrush}" Width="16" Height="16"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource L_Stick_Copy}"
                              Click="OnCopyClick"
                              Style="{DynamicResource Win11MenuItemStyle}">
                        <MenuItem.Icon>
                            <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="12"/>
                        </MenuItem.Icon>
                    </MenuItem>



                    <Separator Style="{DynamicResource Win11MenuSeparatorStyle}"/>

                    <!-- 关闭 -->
                    <MenuItem Header="{DynamicResource L_Stick_Close}" 
                              Click="OnCloseClick"
                              InputGestureText="DblClick"
                              Style="{DynamicResource Win11MenuItemStyle}">
                        <MenuItem.Icon>
                            <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource L_Stick_CloseAll}"
                              Click="OnCloseAllClick"
                              Style="{DynamicResource Win11MenuItemStyle}">
                        <MenuItem.Icon>
                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="12"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </Border.ContextMenu>

            <Border.Effect>
                <DropShadowEffect Color="#CC000000" BlurRadius="22" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>

            <Image x:Name="DisplayImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
        </Border>

        <Button x:Name="HoverCloseButton"
                Width="24"
                Height="24"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,8,8,0"
                Opacity="0"
                Visibility="Collapsed"
                Style="{StaticResource StickyCloseButtonStyle}"
                Click="OnCloseClick"
                ToolTip="Close">
            <Grid>
                <TextBlock Text="✕"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#CC000000"
                           Margin="0,1,0,0"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#D9000000" BlurRadius="3" ShadowDepth="0" Opacity="0.9"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="✕"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Grid>
        </Button>
    </Grid>
</Window>
