<UserControl x:Class="TabPaint.Controls.SearchBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TabPaint.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="28" d:DesignWidth="150"
              x:Name="SearchControlRoot"
              Height="28">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="SearchResultItemStyle" TargetType="ListBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Margin" Value="2,1"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="LayoutRoot" CornerRadius="4"
                                    Background="{TemplateBinding Background}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="LayoutRoot" Property="Background" Value="{DynamicResource SubtleHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="LayoutRoot" Property="Background" Value="{DynamicResource SubtleSelectedBrush}"/>
                                </Trigger>
                                <Trigger Property="IsMouseCaptured" Value="True">
                                    <Setter TargetName="LayoutRoot" Property="Background" Value="{DynamicResource SubtlePressedBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Border x:Name="SearchBorder" CornerRadius="8"
                Background="{DynamicResource ControlBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Path Grid.Column="0" Data="{DynamicResource Search_Image}"
                      Fill="{DynamicResource TextSecondaryBrush}"
                      Width="14" Height="14" Margin="8,0,0,0"
                      VerticalAlignment="Center" Stretch="Uniform"/>

                <Grid Grid.Column="1">
                    <Grid x:Name="PlaceholderGrid" IsHitTestVisible="False">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{DynamicResource L_Settings_SearchPlaceholder}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   FontSize="12" Margin="6,0,0,0"/>
                        <Border Grid.Column="1" Background="#22000000"
                                CornerRadius="3" Padding="6,1"
                                Margin="0,0,6,0" Height="18"
                                VerticalAlignment="Center"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">
                            <TextBlock Text="Ctrl+F"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="10"
                                       VerticalAlignment="Center"/>
                        </Border>
                    </Grid>

                    <TextBox x:Name="SearchTextBox"
                             Margin="6,0,22,0"
                             VerticalAlignment="Center"
                             VerticalContentAlignment="Center"
                             FontSize="12"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             CaretBrush="{DynamicResource TextPrimaryBrush}"
                             Focusable="True"
                             TextChanged="OnSearchTextChanged"
                             GotFocus="OnSearchGotFocus"
                             LostFocus="OnSearchLostFocus"
                             PreviewKeyDown="OnSearchPreviewKeyDown"/>

                    <Border x:Name="FocusBorder"
                            BorderBrush="{DynamicResource SystemAccentBrush}"
                            BorderThickness="0,0,0,2"
                            CornerRadius="4"
                            Visibility="Collapsed"
                            VerticalAlignment="Bottom"
                            Height="2" Margin="0,0,0,-1"/>

                    <Button x:Name="ClearButton"
                            Visibility="Collapsed"
                            Width="18" Height="18"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,2,0"
                            Padding="0"
                            Background="Transparent"
                            Cursor="Hand"
                            IsTabStop="False"
                            Click="OnClearButtonClick">
                        <Path Data="M0,0 L8,8 M8,0 L0,8"
                              Stroke="{DynamicResource TextSecondaryBrush}"
                              StrokeThickness="1.5"
                              Width="10" Height="10"
                              Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- 搜索弹出列表 -->
        <Popup x:Name="SearchPopup" PlacementTarget="{Binding ElementName=SearchTextBox}"
               Placement="Bottom" StaysOpen="True" AllowsTransparency="True"
               PopupAnimation="Slide" MinWidth="260" MaxWidth="320"
               HorizontalOffset="8" VerticalOffset="4">
            <Border CornerRadius="6" Background="{DynamicResource ChromeMediumBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                    MaxHeight="250">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.3"/>
                </Border.Effect>
                <ListBox x:Name="SearchResultList" Background="Transparent" BorderThickness="0"
                         ItemContainerStyle="{StaticResource SearchResultItemStyle}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         SelectionChanged="OnResultListSelectionChanged"
                         PreviewMouseLeftButtonUp="OnResultListPreviewMouseUp">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="local:SearchItem">
                            <Grid Height="36" Width="280">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Path Grid.Column="0" Data="{Binding IconPath}" Stretch="Uniform"
                                      Fill="{DynamicResource IconFillBrush}" Width="14" Height="14"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding DisplayName}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           FontSize="12" VerticalAlignment="Center"/>
                                <Border Grid.Column="2"
                                        Background="{DynamicResource SubtleHoverBrush}"
                                        CornerRadius="3" Padding="6,1"
                                        Margin="4,0,8,0" Height="18"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ParentTag}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="10" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>
