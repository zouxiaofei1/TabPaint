<Window x:Class="TabPaint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TabPaint" 
        xmlns:controls="clr-namespace:TabPaint.Controls"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:gif="https://github.com/XamlAnimatedGif/XamlAnimatedGif"
        Title="TabPaint"
        Height="700" Width="850"
        MinHeight="305" MinWidth="380"
        Top="100" Left="100"
        SizeToContent="Manual"
        Background="Transparent"
        WindowStyle="None"
        SizeChanged="Window_SizeChanged" 
        Closing="Window_Closing"
        PreviewKeyUp="Window_KeyUp"
        x:Name="RootWindow">

    <WindowChrome.WindowChrome>
        <shell:WindowChrome 
            GlassFrameThickness="-1" 
            CaptionHeight="32"
            ResizeBorderThickness="4"
            CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
            <local:ZoomToInverseValueConverter x:Key="ZoomToInverseValueConverter"/>

            <Style x:Key="ViewModeNavButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="Background" Value="{DynamicResource GlassBackgroundMediumBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderMediumBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Opacity" Value="0"/>
                <Setter Property="IsHitTestVisible" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="NavButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="24"
                                    Padding="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="NavButtonBorder" Property="Background" Value="{DynamicResource ToolAccentSubtleHoverBrush}"/>
                                    <Setter TargetName="NavButtonBorder" Property="BorderBrush" Value="{DynamicResource ToolAccentHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="NavButtonBorder" Property="Background" Value="{DynamicResource ToolAccentSubtlePressedBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <Storyboard x:Key="FadeInDragOverlay">
                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="DragOverlay" Storyboard.TargetProperty="Visibility">
                    <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
                </ObjectAnimationUsingKeyFrames>
                <DoubleAnimation Storyboard.TargetName="DragOverlay" Storyboard.TargetProperty="Opacity"
                          From="0" To="1" Duration="0:0:0.05">
                    <DoubleAnimation.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="DragOverlay" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                          From="0.95" To="1" Duration="0:0:0.05">
                    <DoubleAnimation.EasingFunction>
                        <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="DragOverlay" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                          From="0.95" To="1" Duration="0:0:0.05">
                    <DoubleAnimation.EasingFunction>
                        <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

            <Storyboard x:Key="FadeOutDragOverlay">
                <DoubleAnimation Storyboard.TargetName="DragOverlay" Storyboard.TargetProperty="Opacity"
                          To="0" Duration="0:0:0.15"/>
                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="DragOverlay" Storyboard.TargetProperty="Visibility">
                    <DiscreteObjectKeyFrame KeyTime="0:0:0.15" Value="{x:Static Visibility.Collapsed}"/>
                </ObjectAnimationUsingKeyFrames>
            </Storyboard>

            <Style x:Key="RulerVisibilityStyle" TargetType="{x:Type local:Ruler}">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding Source={x:Static local:SettingsManager.Instance}, Path=Current.ShowRulers}" Value="True"/>
                            <Condition Binding="{Binding IsViewMode, ElementName=RootWindow}" Value="False"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Visibility" Value="Visible"/>
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>

            <Storyboard x:Key="CyclePulseAnimation">
                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)">
                    <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1.0"/>
                    <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="1.05">
                        <EasingDoubleKeyFrame.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </EasingDoubleKeyFrame.EasingFunction>
                    </EasingDoubleKeyFrame>
                    <EasingDoubleKeyFrame KeyTime="0:0:0.4" Value="1.0">
                        <EasingDoubleKeyFrame.EasingFunction>
                            <QuadraticEase EasingMode="EaseIn"/>
                        </EasingDoubleKeyFrame.EasingFunction>
                    </EasingDoubleKeyFrame>
                </DoubleAnimationUsingKeyFrames>
                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
                    <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1.0"/>
                    <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="1.05">
                        <EasingDoubleKeyFrame.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </EasingDoubleKeyFrame.EasingFunction>
                    </EasingDoubleKeyFrame>
                    <EasingDoubleKeyFrame KeyTime="0:0:0.4" Value="1.0">
                        <EasingDoubleKeyFrame.EasingFunction>
                            <QuadraticEase EasingMode="EaseIn"/>
                        </EasingDoubleKeyFrame.EasingFunction>
                    </EasingDoubleKeyFrame>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
        </ResourceDictionary>
    </Window.Resources>

    <!-- 唯一的顶级 Grid -->
    <Grid x:Name="OuterGrid" Background="Transparent" PreviewDragOver="OnGlobalDragOver" Drop="OnGlobalDrop" PreviewDragLeave="OnGlobalDragLeave">
        <Border x:Name="WindowRootBorder" ClipToBounds="True">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderLightBrush}"/>
                    <Style.Triggers>
                        <!-- Win11 样式 -->
                        <DataTrigger Binding="{Binding IsWin11, ElementName=RootWindow}" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="CornerRadius" Value="0"/>
                        </DataTrigger>
                        <!-- Win10 样式 -->
                        <DataTrigger Binding="{Binding IsWin11, ElementName=RootWindow}" Value="False">
                            <Setter Property="Margin" Value="12"/>
                            <Setter Property="CornerRadius" Value="8"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderMediumBrush}"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect x:Name="WindowShadow" BlurRadius="12" ShadowDepth="0" Opacity="0.4" Color="Black" RenderingBias="Performance"/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            
            <!-- Border 内部唯一的容器 Grid -->
            <Grid x:Name="LayoutRoot">
                <Grid x:Name="LayoutRoots">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- TitleBar -->
                        <RowDefinition Height="Auto"/>
                        <!-- MenuBar -->
                        <RowDefinition Height="Auto"/>
                        <!-- Separator -->
                        <RowDefinition Height="Auto"/>
                        <!-- ToolBar -->
                        <RowDefinition Height="Auto"/>
                        <!-- ImageBar -->
                        <RowDefinition Height="*"/>
                        <!-- Canvas 主区域 -->
                        <RowDefinition Height="Auto"/>
                        <!-- Separator -->
                        <RowDefinition Height="Auto"/>
                        <!-- StatusBar -->
                        <RowDefinition Height="Auto"/>
                        <!-- StatusCommandBar -->
                    </Grid.RowDefinitions>
                    <!-- 1. 自定义标题栏 -->
                    <controls:TitleBarControl x:Name="AppTitleBar"  Grid.Row="0"
                           IsNewStyle="{Binding IsNewStyle, ElementName=RootWindow}"
                           DockPanel.Dock="Top" 
                           MinimizeClick="Minimize_Click"
                          MaximizeRestoreClick="MaximizeRestore_Click"
                          CloseClick="Close_Click"
                          TitleBarMouseDown="Border_MouseLeftButtonDown"
                          ModeSwitchClick="OnTitleBarModeSwitch"
                          NewTabClick="OnNewTabClick"
                          DiscardImageClick="OnDiscardImageClick"
                          DiscardAllClick="OnDiscardAllClick"
                          UndoClick="OnUndoClick"
                          RedoClick="OnRedoClick"
                          SaveClick="OnSaveClick"
                          SettingsClick="OnSettingsClick"
                          WorkingDirectory="{Binding WorkingPathDisplay, ElementName=RootWindow}"
                          TitleBarRightClick="OnTitleBarRightClick">
                        <controls:TitleBarControl.CacheMode>
                            <BitmapCache EnableClearType="True" SnapsToDevicePixels="True"/>
                        </controls:TitleBarControl.CacheMode>
                    </controls:TitleBarControl>
                    
                    
                    <!-- 2. 菜单栏 -->
                    <ContentControl x:Name="MenuBarHolder" DockPanel.Dock="Top" Grid.Row="1">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsViewMode, ElementName=RootWindow}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsNewStyle, ElementName=RootWindow}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                        <ContentControl.CacheMode>
                            <BitmapCache EnableClearType="True"/>
                        </ContentControl.CacheMode>
                    </ContentControl>
                    <!-- 分割线 -->
                    <Rectangle DockPanel.Dock="Top" Height="1" Fill="{DynamicResource BorderLightBrush}" SnapsToDevicePixels="True" Grid.Row="2">
                        <Rectangle.Style>
                            <Style TargetType="Rectangle">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsViewMode, ElementName=RootWindow}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsNewStyle, ElementName=RootWindow}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Rectangle.Style>
                    </Rectangle>

                    <!-- 3. 工具栏 -->
                    <ContentControl x:Name="ToolBarHolder" DockPanel.Dock="Top" Grid.Row="3"
                               Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}">
                    <ContentControl.CacheMode>
                        <BitmapCache EnableClearType="True"/>
                    </ContentControl.CacheMode>
                    </ContentControl>
                    <!-- 4. 图片列表栏 -->
                    <ContentControl x:Name="ImageBarHolder" DockPanel.Dock="Top" IsTabStop="False" Focusable="False" Grid.Row="4">
                        <ContentControl.CacheMode>
                            <BitmapCache EnableClearType="True"/>
                        </ContentControl.CacheMode>
                    </ContentControl>
                    
                    <!-- 5. 状态栏命令行（双击状态栏展开） -->
                    <Border x:Name="StatusCommandBar" Grid.Row="8"
                            DockPanel.Dock="Bottom"
                            Height="38"
                            Margin="8,0,8,8"
                            Padding="10,5"
                            CornerRadius="8"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource BorderMediumBrush}"
                            Background="{DynamicResource GlassBackgroundMediumBrush}"
                            Visibility="Collapsed">
                        <TextBox x:Name="StatusCommandTextBox"
                                 Height="26"
                                 VerticalContentAlignment="Center"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Foreground="{DynamicResource TextControlBrush}"
                                 CaretBrush="{DynamicResource TextControlBrush}"
                                 FontSize="12"
                                 Text=""
                                 KeyDown="OnStatusCommandTextBoxKeyDown"/>
                        <Border.CacheMode>
                            <BitmapCache EnableClearType="True"/>
                        </Border.CacheMode>
                    </Border>

                    <!-- 6. 底部状态栏 -->
                    <ContentControl x:Name="StatusBarHolder" DockPanel.Dock="Bottom" Grid.Row="7"
                               Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}">
                    <ContentControl.CacheMode>
                        <BitmapCache EnableClearType="True"/>
                    </ContentControl.CacheMode>
                    </ContentControl>
                    <Rectangle Grid.Row="6" Height="1" Fill="{DynamicResource BorderLightBrush}" SnapsToDevicePixels="True"/>

                    <!-- 6. 主画布区域 -->
                    <Grid ClipToBounds="False" Background="Transparent" Grid.Row="5">
                        <Grid.Resources>
                            <local:ScaleToTileRectConverter x:Key="ScaleToTileRectConverter"/>
                        </Grid.Resources>

                        <Grid ClipToBounds="False">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <local:Ruler x:Name="RulerTop" Grid.Row="0" Grid.Column="1" Height="20" Orientation="Horizontal" Style="{StaticResource RulerVisibilityStyle}" PreviewMouseMove="OnRulerMouseMove"/>
                            <local:Ruler x:Name="RulerLeft" Grid.Row="1" Grid.Column="0" Width="20" Orientation="Vertical" Style="{StaticResource RulerVisibilityStyle}" PreviewMouseMove="OnRulerMouseMove"/>
                            
                            <Border Grid.Row="0" Grid.Column="0" Background="{DynamicResource GlassBackgroundMediumBrush}" ClipToBounds="True">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Visibility, ElementName=RulerTop}" Value="Visible">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>

                            <ScrollViewer Grid.Row="1" Grid.Column="1" ContextMenuOpening="OnScrollContainerContextMenuOpening"  
                                  Style="{StaticResource FluentScrollViewer}" Name="ScrollContainer" 
                                  FocusVisualStyle="{x:Null}" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" 
                                  PreviewMouseWheel="OnMouseWheelZoom" Background="Transparent" 
                                  PreviewMouseDown="OnScrollContainerMouseDown" PreviewMouseMove="OnScrollContainerMouseMove"  
                                  PreviewMouseUp="OnScrollContainerMouseUp" MouseDoubleClick="OnScrollContainerDoubleClick" 
                                  AllowDrop="True" ScrollChanged="ScrollContainer_ScrollChanged" 
                                  IsManipulationEnabled="True" ManipulationStarting="OnManipulationStarting" ManipulationDelta="OnManipulationDelta">

                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center" ClipToBounds="False">
                                    <Grid.Resources>
                                        <Thickness x:Key="BigMargin">50,50,50,50</Thickness>
                                        <Thickness x:Key="SmallMargin">5,5,5,5</Thickness>
                                    </Grid.Resources>
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Margin" Value="{StaticResource BigMargin}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsViewMode, ElementName=RootWindow}" Value="True">
                                                    <Setter Property="Margin" Value="{StaticResource SmallMargin}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Grid Name="CanvasWrapper" ClipToBounds="False" HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="True" UseLayoutRounding="True">
                                        <Grid.Background>
                                            <DrawingBrush TileMode="Tile" ViewportUnits="Absolute">
                                                <DrawingBrush.Viewport>
                                                    <Binding ElementName="ZoomTransform" Path="ScaleX" Converter="{StaticResource ScaleToTileRectConverter}" ConverterParameter="20"/>
                                                </DrawingBrush.Viewport>
                                                <DrawingBrush.Drawing>
                                                    <DrawingGroup>
                                                        <GeometryDrawing Brush="{DynamicResource ChromeMediumBrush}">
                                                            <GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,20,20"/></GeometryDrawing.Geometry>
                                                        </GeometryDrawing>
                                                        <GeometryDrawing Brush="{DynamicResource CheckerboardDarkBrush}">
                                                            <GeometryDrawing.Geometry>
                                                                <GeometryGroup>
                                                                    <RectangleGeometry Rect="0,0,10,10"/>
                                                                    <RectangleGeometry Rect="10,10,10,10"/>
                                                                </GeometryGroup>
                                                            </GeometryDrawing.Geometry>
                                                        </GeometryDrawing>
                                                    </DrawingGroup>
                                                </DrawingBrush.Drawing>
                                            </DrawingBrush>
                                        </Grid.Background>

                                        <Grid.LayoutTransform>
                                            <TransformGroup>
                                                <ScaleTransform x:Name="ZoomTransform" ScaleX="1" ScaleY="1" />
                                                <RotateTransform x:Name="VisualRotateTransform" Angle="0" />
                                            </TransformGroup>
                                        </Grid.LayoutTransform>
                                        
                                        <Grid Name="ImageContentLayer" ClipToBounds="True">
                                            <Image Name="BackgroundImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                                            <Image Name="WatermarkPreviewLayer" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" SnapsToDevicePixels="True" IsHitTestVisible="False" Visibility="Collapsed" RenderOptions.BitmapScalingMode="Linear"/>
                                            <Image Name="GifPlayerImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top" SnapsToDevicePixels="True" Visibility="Collapsed" gif:AnimationBehavior.AutoStart="True" gif:AnimationBehavior.RepeatBehavior="Forever" />
                                            <Canvas Name="OcrOverlayCanvas" Background="{x:Null}" Visibility="Collapsed" IsHitTestVisible="False" Panel.ZIndex="70">
                                                <Canvas.Width><Binding ElementName="BackgroundImage" Path="ActualWidth"/></Canvas.Width>
                                                <Canvas.Height><Binding ElementName="BackgroundImage" Path="ActualHeight"/></Canvas.Height>
                                            </Canvas>
                                            <Canvas Name="EditorOverlayCanvas" Background="Transparent" IsHitTestVisible="False" Visibility="Visible"/>
                                            <Image Name="SelectionPreview" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" IsHitTestVisible="False" RenderOptions.BitmapScalingMode="NearestNeighbor" UseLayoutRounding="True" RenderOptions.EdgeMode="Aliased" Margin="0" SnapsToDevicePixels="True" Visibility="Collapsed"/>
                                        </Grid>
                                    </Grid>

                                    <Canvas Name="SelectionOverlayCanvas" Panel.ZIndex="100" Visibility="Collapsed" IsHitTestVisible="False" Background="Transparent" UseLayoutRounding="False" SnapsToDevicePixels="False">
                                        <Canvas.LayoutTransform><Binding ElementName="CanvasWrapper" Path="LayoutTransform"/></Canvas.LayoutTransform>
                                        <Canvas.Width><Binding ElementName="BackgroundImage" Path="ActualWidth"/></Canvas.Width>
                                        <Canvas.Height><Binding ElementName="BackgroundImage" Path="ActualHeight"/></Canvas.Height>
                                    </Canvas>

                                    <Border Name="StaticCanvasBorder" Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}" IsHitTestVisible="False" SnapsToDevicePixels="True" UseLayoutRounding="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1">
                                        <Border.Margin><Thickness Left="-1" Top="-1" Right="-1" Bottom="-1"/></Border.Margin>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Effect" Value="{x:Null}"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsViewMode, ElementName=RootWindow}" Value="False"/>
                                                            <Condition Binding="{Binding ElementName=ZoomTransform, Path=ScaleX, Converter={StaticResource ScaleToTileRectConverter}, ConverterParameter=1.5_IsLess}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="0" BlurRadius="20" Opacity="0.25" RenderingBias="Performance"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>

                                    <Canvas Name="CanvasResizeOverlay" Visibility="Visible" IsHitTestVisible="True" Background="{x:Null}" UseLayoutRounding="False" SnapsToDevicePixels="False" ClipToBounds="False">
                                        <Canvas.LayoutTransform><Binding ElementName="CanvasWrapper" Path="LayoutTransform"/></Canvas.LayoutTransform>
                                    </Canvas>
                                </Grid>
                            </ScrollViewer>
                        </Grid>

                        <Grid x:Name="ViewModeNavOverlay"
                              Panel.ZIndex="120"
                              Background="{x:Null}"
                              Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource BoolToVis}}">
                            <Border x:Name="PrevImageHoverZone"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Width="90"
                                    Height="150"
                                    Background="Transparent"/>

                            <Border x:Name="NextImageHoverZone"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Width="90"
                                    Height="150"
                                    Background="Transparent"/>

                            <Button x:Name="PrevImageButton"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Margin="12,0,0,0"
                                    PreviewMouseLeftButtonDown="OnViewModeNavButtonPreviewMouseLeftButtonDown"
                                    PreviewMouseLeftButtonUp="OnViewModeNavButtonPreviewMouseLeftButtonUp"
                                    MouseLeave="OnViewModeNavButtonMouseLeave"
                                    LostMouseCapture="OnViewModeNavButtonLostMouseCapture"
                                    RenderTransformOrigin="0.5,0.5"
                                    Click="OnViewModePrevImageClick"
                                    ToolTip="上一张">
                                <Button.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Button.RenderTransform>
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource ViewModeNavButtonStyle}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CanNavigateImages, ElementName=RootWindow}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding CanNavigateImages, ElementName=RootWindow}" Value="True"/>
                                                    <Condition Binding="{Binding IsMouseOver, ElementName=PrevImageHoverZone}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Opacity" Value="0.88"/>
                                                <Setter Property="IsHitTestVisible" Value="True"/>
                                            </MultiDataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding CanNavigateImages, ElementName=RootWindow}" Value="True"/>
                                                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Opacity" Value="0.96"/>
                                                <Setter Property="IsHitTestVisible" Value="True"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Path Width="20"
                                      Height="20"
                                      Stretch="Uniform"
                                      Fill="{DynamicResource TextPrimaryBrush}"
                                      Data="{DynamicResource LeftArrow_Image}"/>
                            </Button>

                            <Button x:Name="NextImageButton"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Margin="0,0,12,0"
                                    PreviewMouseLeftButtonDown="OnViewModeNavButtonPreviewMouseLeftButtonDown"
                                    PreviewMouseLeftButtonUp="OnViewModeNavButtonPreviewMouseLeftButtonUp"
                                    MouseLeave="OnViewModeNavButtonMouseLeave"
                                    LostMouseCapture="OnViewModeNavButtonLostMouseCapture"
                                    RenderTransformOrigin="0.5,0.5"
                                    Click="OnViewModeNextImageClick"
                                    ToolTip="下一张">
                                <Button.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Button.RenderTransform>
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource ViewModeNavButtonStyle}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CanNavigateImages, ElementName=RootWindow}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding CanNavigateImages, ElementName=RootWindow}" Value="True"/>
                                                    <Condition Binding="{Binding IsMouseOver, ElementName=NextImageHoverZone}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Opacity" Value="0.88"/>
                                                <Setter Property="IsHitTestVisible" Value="True"/>
                                            </MultiDataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding CanNavigateImages, ElementName=RootWindow}" Value="True"/>
                                                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Opacity" Value="0.96"/>
                                                <Setter Property="IsHitTestVisible" Value="True"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Path Width="20"
                                      Height="20"
                                      Stretch="Uniform"
                                      Fill="{DynamicResource TextPrimaryBrush}"
                                      Data="{DynamicResource RightArrow_Image}"/>
                            </Button>
                        </Grid>

                        <Rectangle x:Name="ThicknessPreview" IsHitTestVisible="False" Fill="Transparent" Stroke="{DynamicResource ToolAccentBrush}" StrokeThickness="2" Width="40" Height="40" Visibility="Collapsed" Opacity="0.6" StrokeDashArray="3 2" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <Grid x:Name="ToolPanelGrid" HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="15,20,0,5" Width="42" Panel.ZIndex="100">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="10000*" MaxHeight="280" MinHeight="140"/>
                                <RowDefinition Height="15"/>
                                <RowDefinition Height="10000*" MaxHeight="280" MinHeight="140"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="1" x:Name="ThicknessPanel" CornerRadius="12" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderMediumBrush}" BorderThickness="1">
                                <Border.Resources><local:DynamicRangeConverter x:Key="DynamicSizeConverter"/></Border.Resources>
                                <Border.Effect><DropShadowEffect BlurRadius="15" Opacity="0.15" ShadowDepth="2"/></Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                    <Image Grid.Row="0" Source="{DynamicResource Stroke_Width_Image}" Margin="0,12,0,4" Width="16" Height="16"/>
                                    <Slider Grid.Row="1" x:Name="ThicknessSlider" Style="{StaticResource Win11VerticalSliderStyle}" Orientation="Vertical" Minimum="0" Maximum="1" SmallChange="0.005" LargeChange="0.05" IsSnapToTickEnabled="False" Margin="0,0,0,10" Thumb.DragStarted="ThicknessSlider_DragStarted" Thumb.DragCompleted="ThicknessSlider_DragCompleted" ValueChanged="ThicknessSlider_ValueChanged">
                                        <Slider.Value>
                                            <MultiBinding Converter="{StaticResource DynamicSizeConverter}" Mode="TwoWay">
                                                <Binding Path="PenThickness" Mode="TwoWay"/><Binding Path="CurrentToolKey" Mode="OneWay"/>
                                            </MultiBinding>
                                        </Slider.Value>
                                        <Slider.ToolTip>
                                            <ToolTip Background="{DynamicResource TextPrimaryBrush}" Foreground="{DynamicResource TextInverseBrush}" BorderThickness="0" Padding="8,4">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Path=PenThickness, StringFormat={}{0:F0}}"/><TextBlock Text="{DynamicResource L_Main_Unit_Pixel}" Margin="2,0,0,0"/>
                                                </StackPanel>
                                            </ToolTip>
                                        </Slider.ToolTip>
                                    </Slider>
                                </Grid>
                            </Border>

                            <Border Grid.Row="3" x:Name="OpacityPanel" CornerRadius="12" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderMediumBrush}" BorderThickness="1">
                                <Border.Effect><DropShadowEffect BlurRadius="15" Opacity="0.15" ShadowDepth="2"/></Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                    <Image Grid.Row="0" Source="{DynamicResource Opacity_Image}" Margin="0,12,0,4" Width="16" Height="16"/>
                                    <Slider Grid.Row="1" x:Name="OpacitySlider" Style="{StaticResource Win11VerticalSliderStyle}" Orientation="Vertical" Minimum="0" Maximum="1" SmallChange="0.05" LargeChange="0.1" Value="{Binding PenOpacity, Mode=TwoWay}" Margin="0,0,0,10" IsSnapToTickEnabled="False" Loaded="OpacitySlider_Loaded" Thumb.DragStarted="OpacitySlider_DragStarted" Thumb.DragCompleted="OpacitySlider_DragCompleted" ValueChanged="OpacitySlider_ValueChanged">
                                        <Slider.ToolTip>
                                            <ToolTip x:Name="OpacityToolTip" Background="{DynamicResource TextPrimaryBrush}" Foreground="{DynamicResource TextInverseBrush}" BorderThickness="0" Padding="8,4" Placement="Right" HorizontalOffset="10">
                                                <StackPanel Orientation="Horizontal"><TextBlock Text="{Binding PenOpacity, StringFormat={}{0:P0}, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                                            </ToolTip>
                                        </Slider.ToolTip>
                                    </Slider>
                                </Grid>
                            </Border>
                        </Grid>

                        <Border x:Name="BirdEyePanel" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,25,25" Width="140" Height="120" Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource BorderMediumBrush}" BorderThickness="1" CornerRadius="4" Visibility="Collapsed">
                            <Border.ContextMenu>
                                <ContextMenu Style="{DynamicResource Win11ContextMenuStyle}">
                                    <MenuItem Header="{DynamicResource L_BirdEye_Ctx_Hide}" Click="BirdEyeHideMenuItem_Click"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Border.Effect><DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="2"/></Border.Effect>
                            <Grid Margin="5">
                                <Image x:Name="BirdEyeImage" Stretch="Uniform" Source="{Binding ElementName=BackgroundImage, Path=Source}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Canvas x:Name="BirdEyeCanvas" Background="Transparent" Width="{Binding ElementName=BirdEyeImage, Path=ActualWidth}" Height="{Binding ElementName=BirdEyeImage, Path=ActualHeight}" HorizontalAlignment="Center" VerticalAlignment="Center" MouseDown="BirdEyeCanvas_MouseDown" MouseMove="BirdEyeCanvas_MouseMove" MouseUp="BirdEyeCanvas_MouseUp">
                                    <Border x:Name="BirdEyeViewport" BorderBrush="{DynamicResource SystemAccentHoverBrush}" BorderThickness="2" Background="{DynamicResource SystemAccentHoverBrush}" Opacity="0.2" IsHitTestVisible="False"/>
                                </Canvas>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>

                <!-- 浮动层 (必须与 DockPanel 平级) -->
                <ContentControl x:Name="TextToolHolder" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,80,0,0" Panel.ZIndex="200" IsTabStop="False"/>
                <ContentControl x:Name="TaskProgressHolder" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,60" Panel.ZIndex="150" IsTabStop="False" Focusable="False"/>
                <ContentControl x:Name="SelectionToolHolder" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Top" Panel.ZIndex="300" IsTabStop="False" Focusable="False" IsHitTestVisible="True" ClipToBounds="True"/>
                <ContentControl x:Name="SelectionRotateHolder" HorizontalAlignment="Left" VerticalAlignment="Top" Panel.ZIndex="300" IsTabStop="False" Focusable="False" ClipToBounds="True"/>
                <ContentControl x:Name="OcrFloatBarHolder" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,80,0,0" Panel.ZIndex="260" IsTabStop="False" Focusable="False"/>
                <controls:WandToleranceFloat x:Name="WandTolerancePopup" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,80,0,0" Panel.ZIndex="250" IsTabStop="False" Focusable="False"/>
                <controls:QuickFormatPanel x:Name="QuickFormatPopup" Visibility="Collapsed" Panel.ZIndex="500" IsTabStop="False" Focusable="True"/>

                <Border x:Name="ThicknessTip" Background="{DynamicResource TextControlBrush}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30,0,0,0" Visibility="Collapsed">
                    <TextBlock x:Name="ThicknessTipText" Foreground="{DynamicResource TextInverseBrush}" FontSize="12" TextWrapping="NoWrap" TextAlignment="Center"/>
                </Border>

                <Grid x:Name="NotificationLayer" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,120" IsHitTestVisible="False">
                    <Border x:Name="InfoToast" Background="{DynamicResource TextControlBrush}" CornerRadius="20" Padding="20,8" Opacity="0">
                        <Border.Effect><DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.3"/></Border.Effect>
                        <TextBlock x:Name="InfoToastText" Text="{DynamicResource L_Main_Toast_Info}" Foreground="{DynamicResource TextInverseBrush}" FontSize="14" VerticalAlignment="Center"/>
                    </Border>
                </Grid>

                <!-- 拖拽遮罩层 -->
                <Grid x:Name="DragOverlay" Visibility="Collapsed" IsHitTestVisible="False" Background="{DynamicResource GlassBackgroundDragOverlay}" RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform><ScaleTransform ScaleX="1" ScaleY="1"/></Grid.RenderTransform>
                    <Rectangle Stroke="{DynamicResource BorderMediumBrush}" StrokeThickness="3" RadiusX="20" RadiusY="20" Margin="40" StrokeDashArray="4 2"/>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Path x:Name="DragOverlayIcon" Width="64" Height="64" Fill="{DynamicResource TextInverseBrush}" Stretch="Uniform" Margin="0,0,0,20" Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" />
                        <TextBlock x:Name="DragOverlayText" Text="{DynamicResource L_Main_Drag_DropToOpen}" Foreground="{DynamicResource TextInverseBrush}" FontSize="28" FontWeight="Light" TextAlignment="Center"/>
                        <TextBlock x:Name="DragOverlaySubText" Text="{DynamicResource L_Main_Drag_ReleaseToConfirm}" Foreground="{DynamicResource BorderMediumBrush}" FontSize="16" Margin="0,10,0,0" TextAlignment="Center"/>
                    </StackPanel>
                </Grid>
                <controls:FilePreviewPopup x:Name="FilePreview" IsHitTestVisible="False" Panel.ZIndex="1000"/>
            </Grid>
        </Border>
    </Grid>
</Window>
