<Window x:Class="TabPaint.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TabPaint" 
        xmlns:controls="clr-namespace:TabPaint.Controls"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="TabPaint"
        Height="900" Width="1250"
        MinHeight="400" MinWidth="600"
        Top="100" Left="100"
        SizeToContent="Manual"
      AllowsTransparency="False" 
        Background="Transparent"
        Closing="Window_Closing"
        WindowStyle="None"
        SizeChanged="Window_SizeChanged" 
        x:Name="RootWindow">
    
    <WindowChrome.WindowChrome>
        
        <shell:WindowChrome 
            GlassFrameThickness="-1" 
            CaptionHeight="32"
            ResizeBorderThickness="4"
            CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="Transparent" PreviewDragOver="OnGlobalDragOver"
          PreviewDrop="OnGlobalDrop"
          PreviewDragLeave="OnGlobalDragLeave">
        <DockPanel LastChildFill="True">

            <!-- #region 1. 自定义标题栏 (TitleBar) -->
            <controls:TitleBarControl x:Name="AppTitleBar" 
                          DockPanel.Dock="Top" 
                          MinimizeClick="Minimize_Click"
                          MaximizeRestoreClick="MaximizeRestore_Click"
                                      
                          CloseClick="Close_Click"
                                        TitleBarMouseDown="Border_MouseLeftButtonDown"
                             ModeSwitchClick="OnTitleBarModeSwitch" />
            <!-- #endregion -->
            
            <!-- #region 2. 菜单栏 (MenuBar) -->
            <ContentControl x:Name="MenuBarHolder" DockPanel.Dock="Top"
                Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}"/>


            <!-- #endregion -->
            <Rectangle DockPanel.Dock="Top" Height="1" Fill="#EBEBEB" SnapsToDevicePixels="True"        Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}"/>
            <!-- #region 3. 工具栏 (ToolBar) -->
            <ContentControl x:Name="ToolBarHolder" DockPanel.Dock="Top"
                Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}"/>

            <!-- #endregion -->

            <!-- #region 4. 图片列表栏 (ImageBar) -->
            <ContentControl x:Name="ImageBarHolder" DockPanel.Dock="Top"
                IsTabStop="False" Focusable="False"/>
            <!-- #endregion -->
            <Rectangle DockPanel.Dock="Top" Height="1" Fill="#EBEBEB" SnapsToDevicePixels="True"  Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}"/>
            
            <!-- #region 5. 底部状态栏 (StatusBar) -->
            <ContentControl x:Name="StatusBarHolder" DockPanel.Dock="Bottom"
                Visibility="{Binding IsViewMode, ElementName=RootWindow, Converter={StaticResource InverseBoolToVis}}"/>
            <!-- #endregion -->
            <Rectangle DockPanel.Dock="Bottom" Height="1" Fill="#EBEBEB" SnapsToDevicePixels="True"/>
            <!-- #region 6. 主画布区域 (Canvas) -->
            <Grid ClipToBounds="True" Background="Transparent">
                <Grid.Resources>
                    <!-- 引入转换器 (假设命名空间别名为 local) -->
                    <local:ScaleToTileRectConverter x:Key="ScaleToTileRectConverter"/>
                </Grid.Resources>

                <ScrollViewer    Style="{StaticResource FluentScrollViewer}"  Name="ScrollContainer" FocusVisualStyle="{x:Null}" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="OnMouseWheelZoom" Background="Transparent" PreviewMouseDown="OnScrollContainerMouseDown" PreviewMouseMove="OnScrollContainerMouseMove"  PreviewMouseUp="OnScrollContainerMouseUp"     MouseDoubleClick="OnScrollContainerDoubleClick" AllowDrop="True" >
                    <ScrollViewer.ContextMenu>
                        <ContextMenu Style="{StaticResource Win11ContextMenuStyle}">
                            <MenuItem Header="复制" InputGestureText="Ctrl+C" Style="{StaticResource Win11MenuItemStyle}" Click="OnCopyClick"/>
                            <Separator Style="{StaticResource Win11MenuSeparatorStyle}"/>
                            <MenuItem Header="粘贴" InputGestureText="Ctrl+V" Style="{StaticResource Win11MenuItemStyle}" Click="OnPasteClick"/>
                            <MenuItem Header="固定缩放倍数" 
                         IsCheckable="True" 
                         Style="{StaticResource Win11MenuItemStyle}"
                         DataContext="{Binding Source={x:Static local:SettingsManager.Instance}, Path=Current}"
                         IsChecked="{Binding IsFixedZoom, Mode=TwoWay}"/>
                            <MenuItem Header="AI 一键抠图 (RMBG)" Click="OnRemoveBackgroundClick">
                                <MenuItem.Icon>
                                    <!-- 这里可以用 Path 或者 FontIcon -->
                                    <TextBlock Text="🪄" FontFamily="Segoe UI Emoji"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="提取图中文字 (OCR)" Click="OnOcrClick">
                                <MenuItem.Icon>
                                    <!-- 使用 Segoe Fluent Icons 字体图标 (E8C6 是扫描图标) -->
                                    <TextBlock Text="&#xE8C6;" FontFamily="Segoe Fluent Icons" />
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </ScrollViewer.ContextMenu>

                    <Grid Name="CanvasWrapper" HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="True" UseLayoutRounding="True">
                        <Grid.Background>
                            <DrawingBrush TileMode="Tile" ViewportUnits="Absolute">
                                <!-- 核心修复：Viewport 绑定缩放倍数，参数 20 是你原本的基础大小 -->
                                <DrawingBrush.Viewport>
                                    <Binding ElementName="ZoomTransform" Path="ScaleX" 
                                    Converter="{StaticResource ScaleToTileRectConverter}" 
                                    ConverterParameter="20"/>
                                </DrawingBrush.Viewport>
                                <DrawingBrush.Drawing>
                                    <DrawingGroup>
                                        <!-- 浅灰底色 -->
                                        <GeometryDrawing Brush="#FFF0F0F0">
                                            <GeometryDrawing.Geometry>
                                                <RectangleGeometry Rect="0,0,20,20"/>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                        <!-- 深灰格子 -->
                                        <GeometryDrawing Brush="#FFD0D0D0">
                                            <GeometryDrawing.Geometry>
                                                <GeometryGroup>
                                                    <RectangleGeometry Rect="0,0,10,10"/>
                                                    <RectangleGeometry Rect="10,10,10,10"/>
                                                </GeometryGroup>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                    </DrawingGroup>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Grid.Background>

                        <Grid.LayoutTransform>
                            <ScaleTransform x:Name="ZoomTransform" ScaleX="1" ScaleY="1" />
                        </Grid.LayoutTransform>
                        <Image Name="PreviewImage" 
                     Stretch="Fill" 
                     Width="{Binding ElementName=BackgroundImage, Path=ActualWidth}"
                     Height="{Binding ElementName=BackgroundImage, Path=ActualHeight}"
                     HorizontalAlignment="Left" 
                     VerticalAlignment="Top" 
                     IsHitTestVisible="False" 
                     Visibility="Collapsed"
                     RenderOptions.BitmapScalingMode="Linear"/>
                        <!-- 1. 基础图片 -->
                        <Image Name="BackgroundImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                        <!-- 2. 调整大小层 -->
                        <Canvas Name="CanvasResizeOverlay" Visibility="Visible" IsHitTestVisible="True" Background="Transparent"/>
                        <!-- 3. 选区预览层 -->
                        <Image Name="SelectionPreview" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" IsHitTestVisible="False" RenderOptions.BitmapScalingMode="NearestNeighbor" UseLayoutRounding="True" RenderOptions.EdgeMode="Aliased" Margin="0" SnapsToDevicePixels="True" Visibility="Collapsed"/>
                        <!-- 4. 选区UI层 -->
                        <Canvas Name="SelectionOverlayCanvas" Visibility="Collapsed" IsHitTestVisible="False" Background="Transparent" UseLayoutRounding="False" SnapsToDevicePixels="False"/>
                        <!-- 5. 编辑器层 -->
                        <Canvas Name="EditorOverlayCanvas" Background="Transparent" IsHitTestVisible="False" Visibility="Visible"/>
                    </Grid>
                </ScrollViewer>

                <!-- 粗细预览圆 -->
                <Rectangle x:Name="ThicknessPreview" 
            IsHitTestVisible="False" 
            Fill="Transparent" 
            Stroke="Purple" 
            StrokeThickness="2" 
            Width="40" Height="40" 
            Visibility="Collapsed" 
            Opacity="0.6" 
            StrokeDashArray="3 2" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"/>
                <Grid x:Name="ToolPanelGrid" HorizontalAlignment="Left" VerticalAlignment="Stretch" Margin="15,20,0,5" Width="42" Panel.ZIndex="100">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="10000*" MaxHeight="280" MinHeight="140"/>
                        <RowDefinition Height="15"/>
                        <RowDefinition Height="10000*" MaxHeight="280" MinHeight="140"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="1" x:Name="ThicknessPanel" CornerRadius="12" Background="#F3F3F3" BorderBrush="#D0D0D0" BorderThickness="1">
                        <Border.Resources>
                            <local:NonLinearRangeConverter x:Key="SizeConverter"/>
                        </Border.Resources>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" Opacity="0.15" ShadowDepth="2"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <!-- 图标 -->
                            <Image Grid.Row="0" Source="{DynamicResource Stroke_Width_Image}" Margin="0,12,0,4" Width="16" Height="16"/>

                            <!-- Slider -->
                            <Slider Grid.Row="1" x:Name="ThicknessSlider" 
                    Style="{StaticResource Win11VerticalSliderStyle}" 
                    Orientation="Vertical" 
                    Minimum="0" Maximum="1"
                    SmallChange="0.005" LargeChange="0.05"
                    DataContext="{Binding Source={x:Static local:SettingsManager.Instance}, Path=Current}"
                    Value="{Binding PenThickness, Mode=TwoWay, Converter={StaticResource SizeConverter}}" 
                    IsSnapToTickEnabled="False"
                    Margin="0,0,0,10"
                    Thumb.DragStarted="ThicknessSlider_DragStarted" 
                    Thumb.DragCompleted="ThicknessSlider_DragCompleted" 
                    ValueChanged="ThicknessSlider_ValueChanged">
                                <Slider.ToolTip>
                                    <ToolTip Background="Black" Foreground="White" BorderThickness="0" Padding="8,4">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding PenThickness, StringFormat={}{0:F0}}"/>
                                            <TextBlock Text=" 像素" Margin="2,0,0,0"/>
                                        </StackPanel>
                                    </ToolTip>
                                </Slider.ToolTip>
                            </Slider>
                        </Grid>
                    </Border>
                    <Border Grid.Row="3" x:Name="OpacityPanel" CornerRadius="12" Background="#F3F3F3" BorderBrush="#D0D0D0" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" Opacity="0.15" ShadowDepth="2"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 图标 -->
                            <Image Grid.Row="0" Source="{DynamicResource Opacity_Image}" Margin="0,12,0,4" Width="16" Height="16"/>

                            <Slider Grid.Row="1" x:Name="OpacitySlider" 
        Style="{StaticResource Win11VerticalSliderStyle}" 
        Orientation="Vertical" 
        Minimum="0" Maximum="1" 
        SmallChange="0.05" LargeChange="0.1"
        DataContext="{Binding Source={x:Static local:SettingsManager.Instance}, Path=Current}"
        Value="{Binding PenOpacity, Mode=TwoWay}" 
        Margin="0,0,0,10"
        IsSnapToTickEnabled="False"
                                      Loaded="OpacitySlider_Loaded"  
        Thumb.DragStarted="OpacitySlider_DragStarted"
        Thumb.DragCompleted="OpacitySlider_DragCompleted"
        ValueChanged="OpacitySlider_ValueChanged">
                                <Slider.ToolTip>
                                    <ToolTip x:Name="OpacityToolTip" Background="#333" Foreground="White" BorderThickness="0" Padding="8,4" Placement="Right" HorizontalOffset="10">
                                        <StackPanel Orientation="Horizontal">
                                            <!-- 注意：这里增加了 UpdateSourceTrigger 确保拖动时数字实时跳动 -->
                                            <TextBlock Text="{Binding PenOpacity, StringFormat={}{0:P0}, UpdateSourceTrigger=PropertyChanged}"/>
                                            <TextBlock Text="" Margin="2,0,0,0"/>
                                        </StackPanel>
                                    </ToolTip>
                                </Slider.ToolTip>
                            </Slider>
                        </Grid>
                    </Border>

                </Grid>

            </Grid>
            <!-- #endregion -->
        </DockPanel>

        <!-- #region 7. 悬浮面板 (Floating UI) -->

        <Border x:Name="TextEditBar" Visibility="Collapsed" MouseDown="TextEditBar_MouseDown" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,80,0,0" Background="#FAFAFA" BorderBrush="#E5E5E5" BorderThickness="1" CornerRadius="8" Padding="6">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Direction="270" Color="#20000000" Opacity="0.4"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <!-- 字体选择: 初始为空，代码懒加载 -->
                <ComboBox x:Name="FontFamilyBox" 
          Style="{StaticResource FluentComboBoxStyle}" 
          Width="160" 
          IsEditable="True" 
          Background="White"
          SelectionChanged="FontSettingChanged" 
          TextBoxBase.TextChanged="FontSettingChanged">
                </ComboBox>
                <!-- 字号 -->
                <ComboBox x:Name="FontSizeBox" Style="{StaticResource FluentComboBoxStyle}" Width="70" 
                  IsEditable="True" Text="24"
                  SelectionChanged="FontSettingChanged" TextBoxBase.TextChanged="FontSettingChanged">
                    <!-- 常用字号 -->
                    <System:Double>9</System:Double>
                    <System:Double>10</System:Double>
                    <System:Double>11</System:Double>
                    <System:Double>12</System:Double>
                    <System:Double>14</System:Double>
                    <System:Double>18</System:Double>
                    <System:Double>24</System:Double>
                    <System:Double>36</System:Double>
                    <System:Double>48</System:Double>
                    <System:Double>64</System:Double>
                    <System:Double>72</System:Double>
                    <System:Double>96</System:Double>
                </ComboBox>

                <Rectangle Width="1" Height="20" Fill="#E0E0E0" Margin="4,0,8,0"/>

                <!-- 基础样式 -->
                <ToggleButton x:Name="BoldBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="FontSettingChanged" ToolTip="加粗">
                    <TextBlock Text="B" FontWeight="Bold" FontFamily="Segoe UI" FontSize="16"/>
                </ToggleButton>
                <ToggleButton x:Name="ItalicBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="FontSettingChanged" ToolTip="斜体">
                    <TextBlock Text="I" FontStyle="Italic" FontFamily="Times New Roman" FontSize="16"/>
                </ToggleButton>
                <ToggleButton x:Name="UnderlineBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="FontSettingChanged" ToolTip="下划线">
                    <TextBlock Text="U" TextDecorations="Underline" FontFamily="Segoe UI" FontSize="16"/>
                </ToggleButton>
                <ToggleButton x:Name="StrikeBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="FontSettingChanged" ToolTip="删除线">
                    <TextBlock Text="S" TextDecorations="Strikethrough" FontFamily="Segoe UI" FontSize="16"/>
                </ToggleButton>

                <Rectangle Width="1" Height="20" Fill="#E0E0E0" Margin="8,0,8,0"/>

                <!-- 对齐方式 (使用 Path 绘制图标) -->
                <ToggleButton x:Name="AlignLeftBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="TextAlign_Click" Tag="Left" IsChecked="True" ToolTip="左对齐">
                    <Path Data="M0,2 L14,2 M0,6 L10,6 M0,10 L14,10 M0,14 L10,14" Stroke="Black" StrokeThickness="1" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </ToggleButton>
                <ToggleButton x:Name="AlignCenterBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="TextAlign_Click" Tag="Center" ToolTip="居中">
                    <Path Data="M0,2 L14,2 M2,6 L12,6 M0,10 L14,10 M2,14 L12,14" Stroke="Black" StrokeThickness="1" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </ToggleButton>
                <ToggleButton x:Name="AlignRightBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="TextAlign_Click" Tag="Right" ToolTip="右对齐">
                    <Path Data="M0,2 L14,2 M4,6 L14,6 M0,10 L14,10 M4,14 L14,14" Stroke="Black" StrokeThickness="1" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </ToggleButton>

                <Rectangle Width="1" Height="20" Fill="#E0E0E0" Margin="8,0,8,0"/>

                <!-- 背景填充 -->
                <ToggleButton x:Name="TextBackgroundBtn" Style="{StaticResource FluentToggleButtonStyle}" Click="FontSettingChanged" ToolTip="不透明背景">
                    <Grid>
                        <Rectangle Width="14" Height="14" Fill="White" Stroke="#333" StrokeThickness="1"/>
                        <TextBlock Text="A" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                    </Grid>
                </ToggleButton>

            </StackPanel>
        </Border>

    

        <!-- 粗细提示 -->
        <Border x:Name="ThicknessTip" Background="#333" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30,0,0,0" Visibility="Collapsed">
            <TextBlock x:Name="ThicknessTipText" Foreground="White" FontSize="12" TextWrapping="NoWrap" TextAlignment="Center"/>
        </Border>

        <!-- 通知层 -->
        <Grid x:Name="NotificationLayer" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,120" IsHitTestVisible="False">
            <Border x:Name="InfoToast" Background="#A0444444" CornerRadius="20" Padding="20,8" Opacity="0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <TextBlock x:Name="InfoToastText" Text="提示信息" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
            </Border>
        </Grid>
        <!-- #endregion -->
        
        <!-- #region 8. 拖拽遮罩层 (Drag Overlay) -->
        <Grid x:Name="DragOverlay" Visibility="Collapsed" IsHitTestVisible="False" Background="#77111111" RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Grid.RenderTransform>
            <Rectangle Stroke="White" StrokeThickness="3" RadiusX="20" RadiusY="20" Margin="40" StrokeDashArray="4 2"/>

            <!-- 文字和图标内容 -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- 图标 -->
                <Path x:Name="DragOverlayIcon" Width="64" Height="64" Fill="White" Stretch="Uniform" Margin="0,0,0,20"
                      Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" />

                <!-- 提示文字 -->
                <TextBlock x:Name="DragOverlayText" Text="拖放以打开" Foreground="White" FontSize="28" FontWeight="Light" TextAlignment="Center"/>

                <!-- 副标题 -->
                <TextBlock x:Name="DragOverlaySubText" Text="释放鼠标以确认" Foreground="#CCC" FontSize="16" Margin="0,10,0,0" TextAlignment="Center"/>
            </StackPanel>
        </Grid>
        <!-- #endregion -->
    </Grid>
</Window>